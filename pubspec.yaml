name:orderapp
description: sample for github.

publish_to: 'none' 
version: 2.0.0+2

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^4.3.1
  url_launcher: ^5.5.2
  flutter_speed_dial: ^1.2.5
  shared_preferences: ^0.5.8
  share: ^0.6.4+3
  connectivity: ^0.4.9+2
  fluttertoast: ^7.0.2
  cloud_firestore: 0.14.0+2
  firebase_core: ^0.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
